{
	"name": "SessionManagerService",
	"uiName": "Cuentas",
	"privileges": ["identity:internal", "identity:discord", "moderation:internal"],
	"version": "1.0.0",
	"kernelMode": 70,
	"failOnError": true,
	"custom": {
		"baseUrl": "${SESSION_OAUTH_BASE_URL:-}"
	},
	"private": {
		"discordClientId": "${DISCORD_CLIENT_ID}",
		"discordClientSecret": "${DISCORD_CLIENT_SECRET}",
		"googleClientId": "${GOOGLE_CLIENT_ID}",
		"googleClientSecret": "${GOOGLE_CLIENT_SECRET}",
		"cookieDomain": "${SESSION_COOKIE_DOMAIN:-}",
		"changePasswordUrl": "${SESSION_CHANGE_PASSWORD_URL:-}",
		"defaultRedirectUrl": "${SESSION_DEFAULT_REDIRECT_URL:-}",
		"oidc": {
			"issuer": "${ADC_OIDC_ISSUER:-}",
			"tokenTtlSeconds": "${ADC_OIDC_TOKEN_TTL_SECONDS:-900}",
			"loginPath": "/auth/login",
			"clients": [
				{
					"clientId": "netbird",
					"name": "Red privada (NetBird)",
					"redirectUris": [
						"https://${ADC_NETBIRD_DOMAIN:-localhost}:${NETBIRD_ADMIN_PORT:-33073}/auth",
						"https://${ADC_NETBIRD_DOMAIN:-localhost}:${NETBIRD_ADMIN_PORT:-33073}/silent-auth",
						"http://localhost:53000"
					]
				}
			]
		}
	},
	"providers": [
		{
			"name": "security/jwt",
			"custom": {
				"jwtSecret": "${JWT_SECRET}"
			}
		},
		{
			"name": "queue/redis",
			"version": "latest",
			"language": "typescript",
			"custom": {
				"host": "${REDIS_HOST:-localhost}",
				"port": "${REDIS_PORT:-6380}",
				"password": "${REDIS_PASSWORD}",
				"db": 0,
				"keyPrefix": "adc:session:"
			}
		}
	],
	"services": [
		{
			"name": "IdentityManagerService",
			"version": "latest",
			"language": "typescript"
		},
		{
			"name": "ModerationService",
			"version": "latest",
			"language": "typescript",
			"optional": true
		},
		{
			"name": "EndpointManagerService",
			"version": "latest"
		},
		{
			"name": "OperationsService",
			"version": "latest"
		}
	]
}
